# WhisperPaw shell completions for bash.
#
# AUTO-GENERATED by whisperpaw._completions.render() — do not edit by hand.
# Regenerate with:
#     python -c "from whisperpaw._completions import render; print(render('bash'), end='')"
# Subcommands covered: paw-read paw-sound paw-watch paw-zoom
#
# For setup instructions, see:
# https://github.com/raincatgirl/WhisperPaw/blob/main/completions/README.md


_paw_read_flags() {
  COMPREPLY=(--file --clipboard --rate --volume --max-chars --backend --piper-voice --quiet)
}
_paw_complete_paw_read() {
  local cur prev
  cur="${COMP_WORDS[COMP_CWORD]}"
  if [[ "$cur" == --* ]]; then
    COMPREPLY=($(compgen -W '--file --clipboard --rate --volume --max-chars --backend --piper-voice --quiet' -- "$cur"))
    return 0
  fi
  # No subcommands; nothing else to complete.
  return 0
}
complete -F _paw_complete_paw_read paw-read

_paw_sound_flags() {
  COMPREPLY=(--pack --volume --quiet --list-packs --list-events)
}
_paw_complete_paw_sound() {
  local cur prev
  cur="${COMP_WORDS[COMP_CWORD]}"
  if [[ "$cur" == --* ]]; then
    COMPREPLY=($(compgen -W '--pack --volume --quiet --list-packs --list-events' -- "$cur"))
    return 0
  fi
  # No subcommands; nothing else to complete.
  return 0
}
complete -F _paw_complete_paw_sound paw-sound

_paw_watch_flags() {
  COMPREPLY=(--rate --volume --max-lines --include-stderr --quiet --follow)
}
_paw_complete_paw_watch() {
  local cur prev
  cur="${COMP_WORDS[COMP_CWORD]}"
  if [[ "$cur" == --* ]]; then
    COMPREPLY=($(compgen -W '--rate --volume --max-lines --include-stderr --quiet --follow' -- "$cur"))
    return 0
  fi
  # No subcommands; nothing else to complete.
  return 0
}
complete -F _paw_complete_paw_watch paw-watch

# paw-zoom: no completion data yet (build_parser() missing).
_paw_zoom() { :; }
complete -F _paw_zoom paw-zoom 2>/dev/null || true
