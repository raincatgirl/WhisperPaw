# GitHub config dir

CI is managed by maintainers' local tooling for now (the developer PAT
used during scaffold does not include the `workflow` scope). Pull
requests that add a `workflows/` file are welcome — once merged the
maintainer can approve the workflow run from the GitHub UI.
